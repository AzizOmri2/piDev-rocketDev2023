 {% extends 'baseBack.html.twig' %}  

 {% block title %}EnergyBox | promotion {% endblock %} 

{% block body %}
<!-- MAIN CONTENT-->
    <div class="page-wrapper">
        <!-- PAGE CONTAINER-->
        <div class="page-container">
            <div class="main-content">
                <div class="section__content section__content--p30">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col-md-12">
                                <!-- START WHITE BACKGROUND-->
                                <div class="map-data m-b-40">
    <h1>Promotions</h1><br>
    
<div class="table-responsive m-b-40">
                                    <table class="table table-borderless table-data3">
        <thead>
            <tr>
               
                <th>CodePromotion</th>
                <th>ReductionPromotion</th>
                <th>DateExpiration</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for promotion in promotions %}
            <tr>
                
                <td>{{ promotion.codePromotion }}</td>
                <td>{{ promotion.reductionPromotion }}</td>
                <td>{{ promotion.DateExpiration ? promotion.DateExpiration|date('Y-m-d H:i:s') : '' }}</td>
                <td>
  <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">
                <button class="item" data-toggle="tooltip" data-placement="top" title="" data-original-title="Modifier">
                                                            <i class="zmdi zmdi-edit"></i>
                                                         
                                                        </button> </a>
                   <br>
                    <a href="{{ path('app_promotion_show', {'id': promotion.id}) }}">
                 <button class="item" data-toggle="tooltip" data-placement="top" title="" data-original-title="afficher">
                                                            <i class="zmdi zmdi-more"></i>
                                                         
                                                        </button>   </a>
                     {{ include('promotion/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
       
                                        </tbody>
                                        </tbody>
                                    </table>
                                </div>
                                <button class="au-btn au-btn-load" >
                                
                                  <a href="{{ path('app_promotion_new') }}" style="color: white;">Ajouter une promotion</a>
                                </button>
  
     </div>

                                <!-- END WHITE BACKGROUND-->
                            </div>
                        </div>
                        
                        
                        <div class="row">
                            <div class="col-md-12">
                                <div class="copyright">
                                    <p>Copyright © 2023 Colorlib. All rights reserved..</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- END MAIN CONTENT-->
        </div>

    </div>
{% endblock %}
