{% extends 'baseBack.html.twig' %}

{% block title %}EnergyBox | Planning {% endblock %}

{% block body %}

<div class="map-data m-b-40">
        <h3 class="title-3 m-b-30">
            <i class="fas fa-table"></i>Gestion Plannings</h3>
                <table class="table">
                    <thead>
                        <tr>
                            <td><strong>Date</strong></td>
                            <td><strong>Cours</strong></td>
                            <td><strong>Jour</strong></td>
                            <td><strong>Heure Début</strong></td>
                            <td></td>
                        </tr>
                    </thead>
                    <tbody>
                    {% for planning in plannings %}
                        <tr>
                            <td>{{ planning.datePlanning ? planning.datePlanning|date('Y-m-d') : '' }}</td>
                            <td>{{ planning.cours.nomCours }}</td>
                            <td>{{ planning.jourPlanning }}</td>
                            <td>{{ planning.heurePlanning }}</td>
                            <td>
                                <button class="btn btn-outline-secondary"><a href="{{ path('app_planning_crud_show', {'id': planning.id}) }}">Afficher</a></button>
                                <button class="btn btn-outline-secondary"><a href="{{ path('app_planning_crud_edit', {'id': planning.id}) }}">Modifier</a></button>
                                {{ include('planning_crud/_delete_form.html.twig') }}
                            </td>
                        </tr>
                        {% else %}
                            <tr>
                                <td colspan="3">Pas de Planning.</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
                <div align="center" class="card-footer">
                    <br>
                    <button class="btn btn-outline-secondary"><a href="{{ path('app_planning_crud_new') }}">Ajouter Planning</a></button>
                </div>
</div>
                                
{% endblock %}
    
