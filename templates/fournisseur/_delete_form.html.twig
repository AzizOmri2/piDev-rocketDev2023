<div style="display:flex;width:100%;align-items: center;justify-content: center ">
    <form method="post" action="{{ path('app_fournisseur_delete', {'id': fournisseur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fournisseur.id) }}">
        <button class="btn btn-danger">Delete</button>
    </form>
</div>
