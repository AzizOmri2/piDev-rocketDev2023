{% extends 'baseFront.html.twig' %}

{% block title %}EnergyBox | Cours | {{ cour.nomCours }}{% endblock %}

{% block tache %}
    <main>
        <!--? Hero Start -->
        <div class="slider-area2">
            <div class="slider-height2 d-flex align-items-center">
                <div class="container">
                    <div class="row">
                        <div class="col-xl-12">
                            <div class="hero-cap hero-cap2 pt-70 text-center">
                                <h2>Cours | {{ cour.nomCours }}</h2>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Hero End -->
        <!--? Blog Area Start-->
        <section class="services-area pt-100 pb-150">
            <section class="wantToWork-area w-padding">
                <div class="container">
                    <div class="row align-items-end justify-content-between">
                        <div class="col-lg-6 col-md-10 col-sm-10">
                            <div class="section-tittle">
                                <span>Cours</span>
                                <h2>{{ cour.nomCours }}</h2>
                            </div>
                        </div>
                        <div class="col-xl-2 col-lg-2 col-md-3">
                            <a href="/cours/crud/viewCours" class="btn wantToWork-btn f-right">Liste des Cours</a>
                        </div>
                    </div>
                </div>
            </section>
            <div class="container">
                <div class="row">
                    <div class="col-lg-8 mb-5 mb-lg-0">       
                        <a class="d-inline-block">
                            <h3>Prix</h3>
                            <p>{{ cour.prixCours }}</p>
                        </a>
                        <br>
                        <a class="d-inline-block">
                            <h3>Coach</h3>
                            <p>{{ cour.nomCoach}}</p>
                        </a>
                        <br>
                        <a class="d-inline-block">
                            <h3>Activités</h3>
                            <p>{{ cour.activites|join(' | ') }}</p>
                        </a>          
                    </div>
                </div>                
            </div>
        </section>
    </main>
{% endblock %}
