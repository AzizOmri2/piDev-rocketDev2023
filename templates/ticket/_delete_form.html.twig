<form method="post" action="{{ path('app_ticket_delete', {'id': ticket.id}) }}" onsubmit="return confirm('Est ce que vous êtes sur que vous souhaitez suprrimer cette ticket ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket.id) }}">
    <button class="btn btn-outline-secondary">Supprimer</button>
</form>
