{% extends 'baseBack.html.twig' %}

{% block title %}EnergyBox | Catégorie {% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ category_reclamation.nomCategory }}</td>
            </tr>
            <tr>
                <th>Priorité</th>
                <td>{{ category_reclamation.prioriteCategory }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <strong>Detail de Catégorie:</strong>
    <br>
    <p>{{ category_reclamation.descriptionCategory }}</p>
    <br>
    <div align="center" class="card-footer">
        <button class="btn btn-outline-secondary"><a href="{{ path('app_category_reclamation_index') }}">Retour à la liste</a></button>
        <button class="btn btn-outline-secondary"><a href="{{ path('app_category_reclamation_edit', {'id': category_reclamation.id}) }}">Modifier</a></button>
        {{ include('category_reclamation/_delete_form.html.twig') }}
    </div>

    
{% endblock %}
